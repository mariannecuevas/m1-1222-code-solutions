/* exported drop */
// Define a function named 'drop' that takes two arguments, array and count
//    Return array with count elements removed.
function drop(array, count) {
  return array.slice(count);
}
