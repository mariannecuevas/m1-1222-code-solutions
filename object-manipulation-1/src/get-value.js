/* exported getValue */
// Define a function named getValue, that takes two arguments, object and key.
//    Return the value of the object at the key variable.
function getValue(object, key) {
  return object[key];
}
