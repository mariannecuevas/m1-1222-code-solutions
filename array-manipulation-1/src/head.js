/* exported head */
// Define a function named head that accepts one argument, array
//    Declare a variable firstElement and assign it to the ordered list at index 0
//    Return the value of firstElement variable
function head(array) {
  var firstElement = array[0];
  return firstElement;
}
